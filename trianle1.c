// compute the area of triangle .

#include <stdio.h>

int main()
{
    // The values o triangles should be integer type.
    int base = 3,height =4;
    int area;

    area = 0.5 * base * height;

    printf("area of triangle: %d\n",area);

    return 0;
}